database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  # Alembic automatically renames the legacy taiwanreservoir database.
  name: sunset

# Optional. Consumed only by sunset-api (Rust). Defaults: 0.0.0.0:8080.
server:
  host: 0.0.0.0
  port: 8080
